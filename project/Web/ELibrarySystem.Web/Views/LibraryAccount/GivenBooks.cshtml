@{
    ViewData["Title"] = "GivenBooks";
}
@model ELibrarySystem.Web.ViewModels.LibraryAccount.GivenBooksViewModel

<div class="container">
    <h2>Всички добавени книга</h2><br />
    <h3>@ViewData["message"]</h3>
    <hr />
    <form asp-action="GivenBooks" asp-route-returnUrl="@ViewData["URL"]">

        <div class="row">
            <div class="col-md-12">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group row">
                    <label asp-for="SearchGivenBook.BookName" class="control-label col-md-2">Заглавие на книгата</label>
                    <input asp-for="SearchGivenBook.BookName" class="form-control col-md-6" />
                    <span asp-validation-for="SearchGivenBook.BookName" class="text-danger col-md-4"></span>
                </div>
                <div class="form-group row">
                    <label asp-for="SearchGivenBook.Author" class="control-label col-md-2">Автор</label>
                    <input asp-for="SearchGivenBook.Author" class="form-control col-md-6" />
                    <span asp-validation-for="SearchGivenBook.Author" class="text-danger col-md-4"></span>
                </div>

                <div class="form-group row">
                    <label asp-for="SearchGivenBook.GenreId" class="control-label  col-md-2">Жанр</label>

                    <select asp-for="SearchGivenBook.GenreId" class="col-md-6">
                        @if (Model.Genres != null)
                        {
                            @foreach (var genre in Model.Genres)
                            {
                                <option value="@genre.Id">@genre.Name</option>

                            }
                        }

                    </select>
                    <span asp-validation-for="SearchGivenBook.GenreId" class="text-danger col-md-4"></span>
                </div>

                <div class="form-group row">
                    <label asp-for="SearchGivenBook.Email" class="control-label col-md-2">Email</label>
                    <input asp-for="SearchGivenBook.Email" class="form-control col-md-6" />
                    <span asp-validation-for="SearchGivenBook.Email" class="text-danger col-md-4"></span>
                </div>

                <div class="form-group row">
                    <label asp-for="SearchGivenBook.FirstName" class="control-label col-md-2">Име</label>
                    <input asp-for="SearchGivenBook.FirstName" class="form-control col-md-6" />
                    <span asp-validation-for="SearchGivenBook.FirstName" class="text-danger col-md-4"></span>
                </div>

                <div class="form-group row">
                    <label asp-for="SearchGivenBook.LastName" class="control-label col-md-2">Фамилия</label>
                    <input asp-for="SearchGivenBook.LastName" class="form-control col-md-6" />
                    <span asp-validation-for="SearchGivenBook.LastName" class="text-danger col-md-4"></span>
                </div>



                <div class="form-group row">
                    <label asp-for="SortMethodId" class="control-label  col-md-2">Сортирай по</label>

                    <select asp-for="SortMethodId" class="col-md-6">
                        @foreach (var SortMethod in Model.SortMethods)
                        {
                            <option value="@SortMethod">@SortMethod</option>
                        }
                    </select>
                    <span asp-validation-for="SortMethodId" class="text-danger col-md-4"></span>
                </div>

                <div class="form-group row">
                    <label asp-for="CountBooksOfPage" class="control-label  col-md-2">Брой книги на една станица</label>

                    <select asp-for="CountBooksOfPage" class="col-md-6">
                        @foreach (var count in Model.CountBooksOfPageList)
                        {
                            <option value="@count">@count</option>

                        }
                    </select>
                    <span asp-validation-for="CountBooksOfPage" class="text-danger col-md-4"></span>
                </div>
                <div class="form-group">
                    <input type="submit" value="Търси" asp-action="GivenBooksSearch" class="btn btn-default" />

                </div>


            </div>
        </div>
        @if (Model.Books != null)
        {
            @foreach (var book in @Model.Books)
            {
                <div class="row">

                    @book.UserName @book.BookName @book.Author @book.GenreName @book.Email<br />
                    <input type="submit" value="Връщане на книга" asp-action="ReturnigGivenBook" asp-route-id="@book.Id" class="btn btn-default" />
                    <input type="submit" value="Премахни" asp-action="DeleteGivenBook" asp-route-id="@book.Id" class="btn btn-default" />
                    <br />

                </div>
            }
            <div class="row">
                <div class="btn-toolbar" role="toolbar" aria-label="Toolbar with button groups">
                    <div class="btn-group" role="group" aria-label="First group">
                        @if (Model.CurrentPage > 1)
                        {
                            @if (Model.CurrentPage >= 2)
                            {
                                <input type="submit" value="|<" asp-route-id="1" asp-action="ChangePageGivenBooks" class="btn btn-default" />
                            }
                            <input type="submit" value="<" asp-route-id="@(Model.CurrentPage-1)" asp-action="ChangePageGivenBooks" class="btn btn-default" />

                        }

                    </div>

                    <pre> </pre>
                    <div class="btn-group" role="group" aria-label="Last Pages group">
                        @for (int i = Math.Max(Model.CurrentPage - 5, 1); i < Model.CurrentPage; i++)
                        {
                            <input type="submit" value="@i" asp-route-id="@i" asp-action="ChangePageGivenBooks" class="btn btn-default" />
                        }
                    </div>
                    <pre> </pre>
                    <div class="btn-group" role="group" aria-label="Active Page group">
                        <input type="submit" value="@Model.CurrentPage" asp-route-id="@Model.CurrentPage" asp-action="ChangePageGivenBooks" class="btn btn-default" />

                    </div>
                    <pre> </pre>
                    <div class="btn-group" role="group" aria-label="Upcoming Page group">
                        @for (int i = Model.CurrentPage + 1; i <= Math.Min(Model.CurrentPage + 5, Model.MaxCountPage); i++)
                        {
                            <input type="submit" value="@i" asp-route-id="@i" asp-action="ChangePageGivenBooks" class="btn btn-default" />

                        }

                    </div>
                    <pre> </pre>
                    <div class="btn-group" role="group" aria-label="Fourth group">
                        <div class="btn-group" role="group" aria-label="First group">
                            @if (Model.CurrentPage < Model.MaxCountPage)
                            {
                                <input type="submit" value=">" asp-route-id="@(Model.CurrentPage+1)" asp-action="ChangePageGivenBooks" class="btn btn-default" />
                                @if (Model.CurrentPage < Model.MaxCountPage - 1)
                                {
                                    <input type="submit" value=">|" asp-route-id="@Model.MaxCountPage" asp-action="ChangePageGivenBooks" class="btn btn-default" />
                                }

                            }

                        </div>
                    </div>
                </div>
            </div>
        }
        else
        {
            <div class="row">
                <div class="col-md-12">
                    Все още нямате добавена книга към Вашата библиотека
                </div>
            </div>
        }
    </form>

</div>